import { configurationHash, simulationVersion } from './version';

export const challengeManifest = Object.freeze({
  challengeId: 'development-movement-lab',
  date: '2026-08-03',
  seed: 'gravity-run:movement-lab:001',
  simulationVersion,
  configurationHash,
  biome: 'shattered-vertical-city',
  mode: 'development',
  expiresAt: null,
});
