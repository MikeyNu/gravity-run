{
  "name": "@gravity-run/asset-pipeline",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit --pretty false",
    "validate": "tsx src/validateManifest.ts ../../content/manifests/core-assets.json",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "tsx": "^4.20.6",
    "typescript": "^7.0.2"
  }
}
