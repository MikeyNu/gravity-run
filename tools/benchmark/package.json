{
  "name": "@gravity-run/benchmark",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit --pretty false",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
